# Xweather Skill

Queries the Xweather Data API and Raster Maps for data.

Since the Data API can return a lot of data, it always writes temporary Python scripts to query, filter and process the data before returning results.
