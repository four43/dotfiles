# Point SSH_AUTH_SOCK at the stable, systemd-managed ssh-agent socket so every
# user session (graphical + SSH login) finds the same agent at a known path.
# The agent is started on demand by ssh-agent.socket (systemd user unit).
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
