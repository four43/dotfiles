#!/bin/bash
# Fastfetch-based login banner.
# When symlinked into /etc/profile.d/, sourced on SSH + console login.
# The interactive guard suppresses output for scp/sftp/non-interactive sessions.

case $- in
    *i*) command -v fastfetch >/dev/null 2>&1 && fastfetch ;;
esac
